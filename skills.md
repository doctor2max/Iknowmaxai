# 🏛️ دليل البنية الهندسية للموقع (Architecture & Skills Guide)

هذا الملف يمثل "حارس الهندسة المعمارية" (Architecture Guard) لمنصة الأكاديمية. يحتوي على كافة التفاصيل التقنية، هيكل الملفات، قاعدة البيانات، والخصائص البرمجية ليكون مرجعاً شاملاً لأي تعديلات أو إضافات مستقبلية.

---

## 🛠️ التقنيات المستخدمة (Tech Stack)

### واجهة المستخدم (Frontend)
* **إطار العمل:** React 19 (عبر Vite)
* **لغة البرمجة:** TypeScript
* **التوجيه (Routing):** React Router v7
* **التصميم (Styling):** Tailwind CSS v4
* **الأيقونات:** Lucide React
* **مكتبات مساعدة:** 
  * `react-markdown`: لعرض محتوى الدروس المكتوب بصيغة Markdown.
  * `react-syntax-highlighter`: لتلوين وعرض الأكواد البرمجية (مثل بايثون).
  * `clsx` & `tailwind-merge`: لدمج وإدارة الفئات (Classes) الخاصة بـ Tailwind.

### الخادم (Backend)
* **بيئة التشغيل:** Node.js
* **إطار العمل:** Express.js
* **لغة البرمجة:** TypeScript (`tsx` للتشغيل في بيئة التطوير)
* **قاعدة البيانات:** SQLite (عبر مكتبة `better-sqlite3`)
* **المصادقة (Authentication):** JWT (JSON Web Tokens) و `bcryptjs` لتشفير كلمات المرور.
* **رفع الملفات:** `multer` لإدارة رفع الصور (مثل صورة الصفحة الرئيسية).

---

## 🗄️ قاعدة البيانات (Database Schema)

قاعدة البيانات عبارة عن ملف محلي باسم `academy.db` (SQLite). تتكون من 4 جداول رئيسية:

### 1. جدول المستخدمين (`users`)
* `id`: معرف فريد (Primary Key).
* `username`: اسم المستخدم (فريد - Unique).
* `password_hash`: كلمة المرور مشفرة (Bcrypt).

### 2. جدول السلاسل التعليمية (`series`)
* `id`: معرف فريد (Primary Key).
* `title`: عنوان السلسلة (مثال: دورة بايثون للمبتدئين).
* `description`: وصف السلسلة.
* `order_index`: ترتيب السلسلة في القائمة الجانبية.

### 3. جدول الدروس (`lessons`)
* `id`: معرف فريد (Primary Key).
* `series_id`: معرف السلسلة التابع لها الدرس (Foreign Key).
* `title`: عنوان الدرس.
* `slug`: الرابط اللطيف للدرس (مثال: `python-variables`).
* `youtube_id`: معرف فيديو اليوتيوب المرفق.
* `markdown_summary`: محتوى الدرس النصي (Markdown).
* `python_code`: كود بايثون للعرض في قسم الأكواد.
* `custom_html`: كود HTML مخصص (يُعرض في Sandbox تفاعلي).
* `download_url`: رابط تحميل المرفقات.
* `order_index`: ترتيب الدرس داخل السلسلة.

### 4. جدول الإعدادات (`settings`)
* `key`: مفتاح الإعداد (Primary Key) (مثل: `academy_title`, `home_image_url`).
* `value`: قيمة الإعداد.

---

## 📂 هيكل الملفات (Project Structure)

```text
/
├── server.ts                 # نقطة الدخول للخادم (Backend) وتعريف مسارات الـ API وقاعدة البيانات
├── academy.db                # ملف قاعدة البيانات (يتم إنشاؤه تلقائياً)
├── package.json              # الاعتماديات وسكربتات التشغيل
├── vite.config.ts            # إعدادات Vite
├── public/
│   └── uploads/              # المجلد الذي تُحفظ فيه الصور المرفوعة من لوحة التحكم
└── src/
    ├── App.tsx               # المكون الرئيسي وتوجيهات (Routes) الواجهة الأمامية
    ├── main.tsx              # نقطة الدخول لتطبيق React
    ├── index.css             # ملف التصميم العام (Tailwind)
    ├── lib/
    │   └── utils.ts          # دوال مساعدة (مثل دمج كلاسات Tailwind)
    ├── components/
    │   ├── Layout.tsx        # الهيكل العام للموقع (يحتوي على القائمة الجانبية والمحتوى)
    │   └── Sidebar.tsx       # القائمة الجانبية (تعرض السلاسل والدروس)
    └── pages/
        ├── HomePage.tsx      # الصفحة الرئيسية (تعرض الترحيب والصورة المرفوعة)
        ├── LessonPage.tsx    # صفحة عرض الدرس (فيديو، نص، كود، HTML Sandbox)
        ├── AdminLogin.tsx    # صفحة تسجيل دخول المدير
        └── AdminDashboard.tsx# لوحة تحكم الإدارة (إدارة السلاسل، الدروس، الإعدادات، ورفع الصور)
```

---

## 🚀 الخصائص والميزات (Features)

### 1. واجهة المستخدم (Public View)
* **الصفحة الرئيسية:** تعرض عنوان الأكاديمية وصورة ترحيبية يتم التحكم بهما من لوحة التحكم.
* **القائمة الجانبية (Sidebar):** تجلب السلاسل والدروس من قاعدة البيانات وتعرضها بشكل هرمي (Accordion).
* **صفحة الدرس:** 
  * مشغل فيديو يوتيوب مدمج.
  * محتوى نصي يدعم تنسيق Markdown.
  * عارض أكواد (Syntax Highlighter) لأكواد بايثون.
  * بيئة تفاعلية (Sandbox) لعرض أكواد HTML/JS مخصصة للدرس.
  * زر لتحميل مرفقات الدرس.

### 2. لوحة التحكم (Admin Dashboard)
* **المصادقة:** محمية بنظام JWT.
* **إدارة السلاسل:** إضافة، تعديل، وحذف السلاسل التعليمية مع تحديد ترتيبها.
* **إدارة الدروس:** إضافة، تعديل، وحذف الدروس وربطها بالسلاسل.
* **إعدادات الأكاديمية:** 
  * تغيير اسم الأكاديمية.
  * تغيير البريد الإلكتروني وروابط التواصل الاجتماعي.
  * **رفع صورة الصفحة الرئيسية:** إمكانية رفع صورة من الجهاز المحلي (تُحفظ في `public/uploads`) أو وضع رابط خارجي.
* **إدارة الحساب:** تغيير اسم المستخدم وكلمة المرور للمدير.

---

## 🌐 مسارات الخادم (API Endpoints)

### مسارات عامة (Public)
* `GET /api/series`: جلب كافة السلاسل والدروس التابعة لها.
* `GET /api/lessons/:slug`: جلب تفاصيل درس معين.
* `GET /api/sandbox/:slug`: جلب وعرض كود HTML المخصص لدرس معين كصفحة ويب.
* `GET /api/settings`: جلب إعدادات الأكاديمية.

### مسارات الإدارة (Protected - Requires JWT)
* `POST /api/login`: تسجيل الدخول والحصول على Token.
* `GET /api/admin/series`: جلب السلاسل لإدارتها.
* `POST /api/admin/series`: إضافة سلسلة جديدة.
* `PUT /api/admin/series/:id`: تعديل سلسلة.
* `DELETE /api/admin/series/:id`: حذف سلسلة.
* `GET /api/admin/lessons`: جلب الدروس لإدارتها.
* `POST /api/admin/lessons`: إضافة درس جديد.
* `PUT /api/admin/lessons/:id`: تعديل درس.
* `DELETE /api/admin/lessons/:id`: حذف درس.
* `POST /api/admin/settings`: حفظ الإعدادات.
* `POST /api/admin/upload`: رفع صورة (باستخدام Multer).
* `POST /api/admin/credentials`: تغيير بيانات الدخول.

---

## ⚙️ آلية التشغيل (Running the App)

* **في بيئة التطوير (Development):**
  يتم تشغيل الخادم والواجهة الأمامية معاً باستخدام `tsx` و `vite middleware` عبر الأمر:
  ```bash
  npm run dev
  ```
  (يعمل الخادم على المنفذ 3000 ويتولى تقديم واجهة React).

* **في بيئة الإنتاج (Production):**
  1. يتم بناء الواجهة الأمامية: `npm run build` (تُحفظ في مجلد `dist`).
  2. يتم تشغيل الخادم: `npm start` (يقوم الخادم بتقديم الملفات الثابتة من مجلد `dist`).

---
*ملاحظة: هذا الملف يُعتبر المرجع الأساسي للمطورين. أي تغيير جذري في قاعدة البيانات أو هيكل الملفات يجب أن يُوثق هنا.*
